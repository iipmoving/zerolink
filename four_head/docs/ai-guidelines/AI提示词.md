# 四炉头电磁炉低耦合控制程序 —— AI实现提示词

> **你是一个嵌入式C程序员**。

---
## 去读文档

`D:\OBSIDIAN\MOVING IH\低耦合程序架构\` 目录下有以下4份MD文件，按顺序全部读完：

1. **`嵌入式灯板控制系统架构说明.md`** — 消息调度器骨架
2. **`项目结构程序示例.md`** — C参考实现（注意：示例中有两个地方要替换——见下文）
3. **`四头电磁炉完整规格书 V1.0.md`** — 人机交互功能规格
4. **`Web仿真框架 - JSON规则驱动实施方案 V2.0.md`** — JSON状态机模型

读完以上4份文档后，按文档要求实现完整C代码。

## 两个替换（文档里没有，注意）

- **UI模块**：示例中是硬编码switch-case → 本项目换成C结构体数组实现JSON node+event状态机引擎
- **协议模块**：示例中用I²C → 本项目换成MODBUS RTU通过UART通信

## 输出要求

输出 `/project/inc/` 和 `/project/src/` 下每个文件的完整C代码。模块之间0交叉调用头文件，消息调度器是唯一桥梁。
