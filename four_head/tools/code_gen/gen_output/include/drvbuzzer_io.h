/**
 * drvbuzzer_io.h —— DrvBuzzer 输入输出接口定义 (v2.3)
 *
 * 数据流:

 *
 * Include 权限:
 *   - 仅 drvbuzzer.c 和 data_switcher.c 可 include
 *   - 使用全路径: #include "include/drvbuzzer_io.h"
 *
 * 本文件包含 std_module.h，调用者无需再包含
 */
#ifndef DRVBUZZER_IO_H
#define DRVBUZZER_IO_H

#include "../core/std_module.h"
#include <stdint.h>

/* ========== OUTPUT (无) ========== */
/* 本模块没有输出 */

/* ========== INPUT (无) ========== */
/* 本模块没有输入 */

#endif /* DRVBUZZER_IO_H */
