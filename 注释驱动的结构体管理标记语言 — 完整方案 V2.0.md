# 注释驱动的结构体管理标记语言 — 完整方案 V2.0

## 一、背景与目标

### 1.1 背景

在 `__weak` 直调架构下，跨模块通信已实现编译期零耦合——模块间不通过 `#include` 共享结构体定义，而是通过 `__weak` 回调传递数据。但这也带来了新问题：

- **同结构体多副本一致性**：同一个结构体在多个模块各有一份独立声明，如何保证一致？
    
- **字段按需裁剪**：每个模块只需要结构体的部分字段，如何声明“我要哪些、不要哪些”？
    
- **变更溯源**：谁在什么时候改了哪个结构体的哪个字段？为什么改？
    

### 1.2 目标

用一套**基于注释的轻量级标记语言**，让 AI 和 Python 管理工具能读懂每个结构体的“使用说明书”，自动完成结构体多副本一致性维护、字段按需裁剪、变更历史归档。

### 1.3 核心原则

- **指令即注释**：所有管理指令都写在 `.h` 文件的注释中，不侵入代码逻辑
    
- **按需导入**：每个模块只声明自己需要的字段，其他字段用默认值跳过
    
- **自动清空**：Python 工具执行完指令后，自动删除指令注释块
    
- **集中归档**：所有变更记录归档到公共维护文件，形成管理链表
    
- **编译期强制隔离**：无保护的 `.h` 文件只能被一个模块引用，违规即报错
    
- **修改前先校验**：每次执行修改前，强制校验链表与代码一致性
    
- **后缀驱动调用方向**：`_IN`/`_OUT` 后缀规范数据流向，管理工具自动校验
    

## 二、编译期强制隔离：无保护的头文件

### 2.1 设计原理

传统的 `.h` 文件使用 `#ifndef` / `#define` / `#endif` 保护，允许多个 `.c` 文件安全地 `#include` 同一个 `.h`，共享其中的结构体定义。但这也导致了编译期耦合——所有模块都可以自由引用同一个公共结构体。

本方案的做法是：**保留 `#ifndef` 保护符的书写，但将其注释掉。** 这样，编译器不会看到保护符，每个 `.h` 文件只能被一个 `.c` 文件 `#include`。如果第二个模块也尝试 `#include` 同一个 `.h`，链接器会报“重复定义”错误。

### 2.2 代码示例

**传统写法（允许多模块共享）：**

c

#ifndef _GLOBAL_STATE_H_
#define _GLOBAL_STATE_H_
typedef struct {
    uint8_t head_power[4];
} MSG_STATE_GlobalState_OUT;
#endif

**本方案写法（编译期强制隔离）：**

c

// #ifndef _GLOBAL_STATE_H_    ← 保留但注释掉
// #define _GLOBAL_STATE_H_    ← 保留但注释掉
/**
 * @tag MSG_STATE_GlobalState_OUT
 * @message 状态机模块输出的全局状态结构体
 * @create state_module
 * @import display_module MSG_STATE_GlobalState_OUT  head_power[4]
 */
typedef struct {
    uint8_t head_power[4];
} MSG_STATE_GlobalState_OUT;
// #endif  ← 保留但注释掉

### 2.3 效果

- **唯一引用**：只有创建该结构体的主模块可以 `#include` 这个 `.h` 文件
    
- **违规即报错**：如果其他模块也尝试 `#include`，链接器立即报“重复定义”错误
    
- **强制走__weak**：所有跨模块数据访问必须通过 `__weak` 回调，从机制上堵死直接 `#include` 的路
    
- **一眼可查**：注释掉的 `#ifndef` 本身就是醒目的视觉标记，提醒开发者“这个文件有特殊规则”
    

## 三、标记语言完整语法

### 3.1 标签一览

|标签|功能|位置|示例|
|---|---|---|---|
|`@body`|文件总体描述|文件头部|`@body 本文件定义全局状态结构体`|
|`@tag`|唯一标识符|每个声明块|`@tag MSG_STATE_GlobalState_OUT`|
|`@message`|简要说明|每个声明块|`@message 状态机模块输出的全局状态`|
|`@create`|在指定模块创建副本|源结构体定义处|`@create state_module`|
|`@import`|指定模块导入哪些字段|源结构体定义处|`@import display_module MSG_STATE_GlobalState_OUT head_power[4]`|
|`@modify`|发起修改请求|任意模块文件|`@modify MSG_STATE_GlobalState_OUT`|
|`@change`|修改请求的具体变更|任意模块文件|`@change head_power type=uint8_t→uint16_t`|
|`@scope`|修改请求的发起模块|任意模块文件|`@scope key_module`|
|`@affected`|修改请求影响的其他模块|任意模块文件|`@affected display_module`|
|`@reserve`|预留/跳过字段|源结构体定义处|`@reserve MSG_STATE_GlobalState_OUT reserved_1, reserved_2`|
|`@deprecate`|标记字段即将废弃|源结构体定义处|`@deprecate old_field replace=new_field deadline=2026-06-30`|
|`@version`|结构体版本号|源结构体定义处|`@version 2.1`|
|`@rollback`|回滚到指定版本|任意模块文件|`@rollback MSG_STATE_GlobalState_OUT to=1.0`|

### 3.2 标签详细语法

#### @body（文件总体描述）

text

@body <描述文本>

每个 `.h` 文件只能有一个 `@body`，放在文件最顶部的注释中。

#### @tag（唯一标识符）

text

@tag <标识符>

推荐使用完整的命名规范标识符作为标签值，如 `MSG_STATE_GlobalState_OUT`。每个声明块可以有多个 `@tag`，每个标签定义一段独立的声明内容。

#### @message（简要说明）

text

@message <说明文本>

#### @create（创建副本）

text

@create <模块名>                    // 创建完整副本
@create <模块名> partial=true       // 创建部分副本（与@import配合）

#### @import（按需导入字段）

text

@import <模块名> <结构体名> <字段1>, <字段2>, ...
@import display_module MSG_STATE_GlobalState_OUT head_power[4], work_mode

每个模块可以有多条 `@import` 指令，精确到结构体内的具体字段。不需要的字段自动跳过，用预留默认值填充。

#### @modify（发起修改请求）

text

@modify <结构体@tag标识符>
@message <修改原因说明>
@change <字段名> type=<旧类型>→<新类型>
@change <字段名> add=<类型> default=<默认值>
@change <字段名> remove=true
@change <字段名> default=<新默认值>
@scope <发起修改的模块>
@affected <受影响模块1>, <受影响模块2>

任何模块都可以发起对公共结构体的修改请求，不需要跳转到源定义文件中去改。

#### @reserve（预留字段）

text

@reserve <结构体名> <字段1>, <字段2>, ...

预留字段用默认值填充，管理工具保证 `@import` 字段不能出现在 `@reserve` 列表中。

#### @deprecate（标记字段废弃）

text

@deprecate <字段名> replace=<替代字段> deadline=<废弃日期>
@deprecate old_power_level replace=head_power[0] deadline=2026-06-30

#### @version（结构体版本号）

text

@version <版本号>

每次 `@modify` 执行后，版本号自动递增。

#### @rollback（回滚指令）

text

@rollback <结构体@tag标识符> to=<目标版本>
@rollback MSG_STATE_GlobalState_OUT to=1.0

管理工具自动逆向操作，将结构体恢复到指定版本。

### 3.3 完整注释块示例

c

// #ifndef _GLOBAL_STATE_H_
// #define _GLOBAL_STATE_H_
/**
 * @body
 * 本文件定义电磁炉控制系统的全局状态结构体。
 * 该结构体由 state_module 创建和维护，其他模块通过 __weak 回调获取。
 *
 * @tag MSG_STATE_GlobalState_OUT
 * @message 状态机模块输出的全局状态结构体，包含当前工作模式、功率档位、定时器状态等
 * @version 1.2
 *
 * @create state_module
 * @create display_module partial=true
 * @create timer_module   partial=true
 *
 * @import state_module   MSG_STATE_GlobalState_OUT  head_power[4], timer_remaining, work_mode
 * @import display_module MSG_STATE_GlobalState_OUT  seg_chars[8], led_state, blink_phase
 * @import timer_module   MSG_STATE_GlobalState_OUT  timer_remaining, timer_active
 *
 * @reserve MSG_STATE_GlobalState_OUT  reserved_1, reserved_2
 *
 * @deprecate old_power_level replace=head_power[0] deadline=2026-06-30
 */
typedef struct {
    uint8_t head_power[4];      // 4个炉头的功率档位
    uint8_t work_mode;          // 当前工作模式
    uint16_t timer_remaining;   // 定时剩余时间
    uint8_t timer_active;       // 定时是否激活
    uint8_t seg_chars[8];       // 数码管显示内容
    uint8_t led_state;          // LED灯状态位掩码
    uint8_t blink_phase;        // 闪烁相位
    uint8_t reserved_1;         // 预留字段
    uint16_t reserved_2;        // 预留字段
    uint8_t old_power_level;    // @deprecate: 已废弃，请用 head_power[0]
} MSG_STATE_GlobalState_OUT;
// #endif

### 3.4 修改请求示例

c

// 在 key_module.c 中发起修改请求
/**
 * @modify MSG_STATE_GlobalState_OUT
 * @message key_module 需要将 head_power 改为 uint16_t 以支持更高功率档位
 * @change head_power  type=uint8_t→uint16_t
 * @scope key_module
 * @affected state_module, display_module
 */

## 四、后缀驱动的调用方向规范

### 4.1 核心规则

|后缀|含义|谁创建|谁可以使用|调用方向|
|---|---|---|---|---|
|**（无后缀）**|本模块内部使用|本模块|**仅本模块**|不跨模块|
|`_IN`|本模块接收的输入|本模块|发送方模块|其他模块 → 本模块|
|`_OUT`|本模块发出的输出|本模块|接收方模块|本模块 → 其他模块|

### 4.2 调用方向铁律

text

上层模块（APP层）
    │
    ├─ 可以调用下层模块的 _OUT 结构体（下层输出给上层）
    │
    └─ 禁止调用下层模块的无后缀结构体（那是下层内部用的）
       禁止下层模块直接调用上层模块的 _OUT 结构体（只能上调下，不能下调上）

**"只能上调下，不能下调上"的规范**：

- 下层模块（如驱动层）的输出结构体带 `_OUT` 后缀，供上层模块（如APP层）读取
    
- 上层模块的输出结构体也带 `_OUT` 后缀，但下层模块不能直接引用——下层只能通过 `__weak` 回调接收上层指令，不能直接 `#include` 上层的结构体定义
    
- 无后缀的结构体是本模块的“私有财产”，任何其他模块都不能引用
    

### 4.3 与无保护头文件的配合

无保护的 `.h` 文件只有本模块能 `#include`，所以：

- **本模块的无后缀结构体**：自然只能被本模块使用，其他模块无法 `#include`
    
- **本模块的 `_IN` 结构体**：由本模块定义，发送方模块通过 `__weak` 回调的参数类型来引用
    
- **本模块的 `_OUT` 结构体**：由本模块定义，接收方模块通过 `__weak` 回调的参数类型来引用
    

### 4.4 管理工具的自动校验

管理工具扫描所有 `@import` 声明，自动校验调用方向：

|校验项|规则|违规时的报错|
|---|---|---|
|**私有结构体外泄**|无后缀的结构体不能出现在任何其他模块的 `@import` 中|`错误: [MSG_STATE_InternalData] 是无后缀的私有结构体，不能在其他模块中 @import。`|
|**上调下**|上层模块可以 `@import` 下层模块的 `_OUT` 结构体|合法，不报错|
|**下调上**|下层模块不能 `@import` 上层模块的 `_OUT` 结构体|`错误: [MSG_APP_Config_OUT] 是上层模块的输出结构体，下层模块不能直接引用。请通过 __weak 回调获取。`|
|**输入方向**|`_IN` 结构体只能被发送方模块 `@import`|`错误: [MSG_KEY_KeyEvent_IN] 是 key_module 的输入结构体，只有发送按键事件的模块才能 @import。`|

### 4.5 完整示例

c

// ===== display_module 的 .h 文件（无保护）=====
// #ifndef _DISPLAY_DATA_H_
// #define _DISPLAY_DATA_H_
/**
 * @tag MSG_DISPLAY_InternalBuffer
 * @message 显示模块内部缓冲区，仅供本模块使用
 * （无后缀 = 私有，其他模块不能 @import）
 */
typedef struct {
    uint8_t seg_buff[8];
    uint8_t led_buff;
} MSG_DISPLAY_InternalBuffer;
/**
 * @tag MSG_DISPLAY_DisplayData_IN
 * @message 显示模块接收的输入结构体
 * @create display_module
 * @import state_module MSG_DISPLAY_DisplayData_IN  seg_chars[8], led_state
 */
typedef struct {
    uint8_t seg_chars[8];
    uint8_t led_state;
} MSG_DISPLAY_DisplayData_IN;
/**
 * @tag MSG_DISPLAY_Status_OUT
 * @message 显示模块输出的状态结构体，供上层模块读取
 * @create display_module
 * @import state_module MSG_DISPLAY_Status_OUT  blink_phase
 */
typedef struct {
    uint8_t blink_phase;
} MSG_DISPLAY_Status_OUT;
// #endif

## 五、公共命名规范

### 5.1 命名格式

|类型|格式|示例|
|---|---|---|
|**常量/宏定义**|`IDX_<模块名>_<变量名>`|`IDX_STATE_work_mode_STANDBY`|
|**结构体（通用）**|`MSG_<模块名>_<变量名>`|`MSG_STATE_GlobalState`|
|**结构体（输入）**|`MSG_<模块名>_<变量名>_IN`|`MSG_KEY_KeyEvent_IN`|
|**结构体（输出）**|`MSG_<模块名>_<变量名>_OUT`|`MSG_STATE_DisplayData_OUT`|

### 5.2 前缀含义

|前缀|含义|适用场景|
|---|---|---|
|`IDX_`|常量/索引/枚举值|功率档位定义、超时阈值、状态枚举|
|`MSG_`|消息/结构体|跨模块传递的数据结构|

### 5.3 后缀含义

|后缀|含义|说明|
|---|---|---|
|`_IN`|输入|其他模块发送给本模块的数据|
|`_OUT`|输出|本模块发送给其他模块的数据|
|（无后缀）|通用/共享|模块内部使用或双向共享|

### 5.4 模块名列表

|模块名|对应模块|说明|
|---|---|---|
|`STATE`|state_module|状态机模块|
|`KEY`|key_module|按键模块|
|`DISPLAY`|display_module|显示模块|
|`TIMER`|timer_module|定时器模块|

## 六、管理工具（Python程序）

### 6.1 工具职责

Python 管理工具 `struct_manager.py` 负责以下工作：

1. **扫描**：扫描所有模块文件，收集 `@import`、`@modify`、`@create`、`@reserve` 等指令
    
2. **校验**：检查冲突——两个模块对同一字段发起不同修改时报警
    
3. **一致性检查**：校验公共管理链表与实际 `.h` 文件声明是否完全一致
    
4. **方向检查**：校验 `_IN`/`_OUT` 后缀的调用方向是否合规
    
5. **执行**：自动修改源结构体定义，同步更新所有受影响模块的副本
    
6. **清空**：执行完毕后，自动删除所有指令注释块
    
7. **归档**：在公共维护文件中生成变更记录，形成管理链表
    

### 6.2 执行流程

text

扫描所有 .c/.h 文件
    ↓
收集所有 @import、@modify、@create、@reserve、@deprecate 声明
    ↓
一致性校验：公共管理链表 ↔ .h 文件声明
    ↓ 不通过 → 生成差异报告 → 暂停 → 等待人工确认
    ↓ 通过
方向校验：_IN/_OUT 后缀调用方向
    ↓ 不通过 → 生成违规报告 → 暂停 → 等待人工修正
    ↓ 通过
前置条件检查：@create 先于 @import，@import 先于 @modify
    ↓ 不通过 → 生成报错报告 → 暂停
    ↓ 通过
校验冲突：
    - 两个模块对同一字段发起不同修改 → 报错
    - 修改字段名不存在 → 报错
    - 类型变更与现有 @import 不兼容 → 警告
    - @import 字段出现在 @reserve 列表中 → 报错
    ↓
无冲突 → 自动执行修改：
    1. 修改源结构体定义
    2. 同步更新所有受影响模块的副本
    3. 更新 interface_map.h
    4. 更新 @version 版本号
    5. 重新编译验证
    ↓
有冲突 → 生成冲突报告，等待人工裁决
    ↓
清空所有指令注释块（@import、@modify、@create、@reserve、@deprecate、@rollback）
    ↓
在公共管理链表中追加变更记录
    ↓
再次运行一致性校验（确认修改后一致）

### 6.3 指令生命周期

text

写入 → 扫描 → 校验（一致性+方向+前置条件+冲突）→ 执行 → 清空 → 归档 → 再校验

指令只在需要修改时才存在，修改完成后自动消失。代码始终保持干净。

### 6.4 运行模式

bash

# 干运行模式（只检查，不修改）
python struct_manager.py --check
# 执行模式（检查通过后执行修改）
python struct_manager.py --apply
# 自动修复模式（仅限安全修复）
python struct_manager.py --check --fix
# 增量扫描模式（只扫描有变更的文件）
python struct_manager.py --apply --incremental

## 七、操作生命周期与报错机制

### 7.1 操作生命周期

管理工具中的所有操作必须遵循严格的生命周期顺序。违反顺序时，工具必须报错并中止。

text

创建（@create）
    ↓
声明（@import）
    ↓
修改（@modify）
    ↓
废弃（@deprecate）
    ↓
回滚（@rollback）

### 7.2 前置条件检查

每个操作类型都有对应的前置条件。前置条件不满足时，工具必须报错并中止。

|操作|前置条件|违反时的报错|
|---|---|---|
|`@import`|目标结构体必须已被 `@create`|`错误: 结构体 [MSG_XXX] 尚未创建，无法导入。请先执行 @create。`|
|`@modify`|目标结构体必须已被 `@create`|`错误: 结构体 [MSG_XXX] 尚未创建，无法修改。请先执行 @create。`|
|`@modify` 字段|目标字段必须存在于结构体中|`错误: 字段 [field_name] 在结构体 [MSG_XXX] 中不存在。可用字段: [field1, field2, ...]`|
|`@modify` 类型变更|新类型必须与现有 `@import` 兼容|`警告: 字段 [field_name] 类型变更影响模块 [module_name]，该模块 @import 了此字段。建议同步更新。`|
|`@deprecate`|目标字段必须存在于结构体中|`错误: 字段 [field_name] 在结构体 [MSG_XXX] 中不存在，无法标记为废弃。`|
|`@deprecate`|必须提供替代字段或废弃原因|`警告: @deprecate 建议提供 replace= 参数指明替代方案。`|
|`@rollback`|目标版本必须存在于公共管理链表中|`错误: 版本 [1.0] 在结构体 [MSG_XXX] 的变更历史中不存在。可用版本: [1.0, 1.1, 1.2]`|
|`@rollback`|回滚操作不能有冲突的并发修改|`错误: 结构体 [MSG_XXX] 在回滚目标版本之后有未归档的修改，请先处理冲突。`|
|`@import` 字段|字段不能出现在 `@reserve` 列表中|`错误: 字段 [field_name] 已被 @reserve 标记为预留字段，无法 @import。`|
|`@create`|同一结构体不能重复创建|`错误: 结构体 [MSG_XXX] 已在模块 [module_name] 中创建，不能重复创建。`|

### 7.3 报错级别

|级别|说明|工具行为|
|---|---|---|
|**ERROR**|前置条件不满足，操作无法执行|中止当前操作，跳过后续操作|
|**WARNING**|可能存在问题，但操作可以继续|输出警告信息，继续执行|
|**INFO**|提示信息|输出提示，继续执行|

### 7.4 报错输出格式

text

========================================
  struct_manager.py — 执行报告
  时间: 2026-05-27 17:30
  模式: --apply
========================================
ERROR (3):
  [MSG_NONEXISTENT] 结构体尚未创建，无法导入。请先执行 @create。
    位置: display_module.c, 第 15 行
  [MSG_NONEXISTENT] 结构体尚未创建，无法修改。请先执行 @create。
    位置: key_module.c, 第 22 行
  [reserved_1] 已被 @reserve 标记为预留字段，无法 @import。
    位置: display_module.c, 第 18 行
WARNING (1):
  [head_power] 类型变更影响模块 [display_module]，该模块 @import 了此字段。建议同步更新。
    位置: key_module.c, 第 25 行
========================================
  执行结论: 失败 (3 个错误)
  已中止。请修复上述错误后重试。
========================================

## 八、一致性校验机制

### 8.1 校验目标

管理工具在执行任何修改前，必须先校验**公共管理链表**与**实际 `.h` 文件中的声明**是否完全一致。不一致时暂停所有操作，生成差异报告，等待人工确认。

### 8.2 校验时机

|时机|说明|
|---|---|
|**每次执行修改前**|强制校验，不一致则中止|
|**Git 提交前**（预提交钩子）|自动触发校验，不一致则阻止提交|
|**定期巡检**（AI 定时任务）|每日自动扫描，发现不一致立即报警|

### 8.3 校验内容

|校验项|说明|不一致时的处理|
|---|---|---|
|**链表记录 ↔ .h 文件存在性**|链表中每个 `@tag` 是否在某个 `.h` 文件中有对应定义|标记为“孤立记录”|
|**.h 文件 ↔ 链表记录**|每个带 `@tag` 的结构体是否在链表中有对应记录|标记为“未登记结构体”|
|**字段定义一致性**|链表记录的字段名、类型、顺序是否与 `.h` 文件完全一致|逐字段对比，生成差异报告|
|**@import 关系一致性**|各模块中管理工具生成的结构体副本，是否与 `@import` 声明一致|标记为“副本不同步”|
|**@version 版本号一致性**|链表记录的版本号是否与 `.h` 文件中的 `@version` 一致|标记为“版本不一致”|
|**调用方向一致性**|`_IN`/`_OUT` 后缀的使用是否与 `@import` 方向一致|标记为“方向违规”|

### 8.4 差异报告示例

text

========================================
  一致性校验报告 — 2026-05-27 16:30
========================================
✅ 链表记录 ↔ .h 文件: 12/12 匹配
✅ .h 文件 ↔ 链表记录: 4/4 已登记
✅ @version 版本号: 4/4 一致
✅ 调用方向: 8/8 合规
❌ 字段定义不一致 (1处):
  @tag: MSG_STATE_GlobalState_OUT
  字段: head_power
  链表记录: uint16_t head_power[4]
  实际代码: uint8_t head_power[4]
  建议: 更新链表记录或回滚代码修改
⚠️ 孤立记录 (1处):
  @tag: MSG_TIMER_OldParams
  链表中有记录，但代码中未找到对应定义
  建议: 确认该结构体是否已删除，若是则归档该记录
========================================
  校验结论: 不通过 (1处不一致, 1处孤立)
  管理工具已暂停，请处理上述问题后重试。
========================================

### 8.5 自动修复能力

对于可自动修复的不一致，管理工具提供 `--fix` 选项：

**可自动修复：**

- 链表记录的格式错误（如缩进、换行符不一致）
    
- 时间戳格式标准化
    
- 字段顺序调整（以 `.h` 文件为准，自动更新链表记录）
    

**必须人工确认：**

- 字段类型不一致
    
- 字段名称不一致
    
- 链表中有记录但代码中找不到对应结构体
    
- 代码中有结构体但链表无记录
    
- 版本号不一致
    
- 调用方向违规
    

### 8.6 与 AI 的配合

AI 每日定时运行 `--check` 模式，发现不一致时：

1. **自动生成修复建议**：AI 读取差异报告，判断不一致的原因
    
2. **简单问题自动修复**：如字段顺序调整，AI 自行运行 `--fix`
    
3. **复杂问题上报**：如字段类型冲突，AI 生成详细报告，通知人工裁决
    
4. **修复后自动校验**：AI 修复后重新运行 `--check`，确认通过后记录日志
    

## 九、公共管理链表

### 9.1 存放位置与格式

管理链表存放在 `interface_map.md` 或 `interface_map.json` 中，与 `interface_map.h` 放在同一目录。

### 9.2 记录内容

每条变更记录包含：

- **时间戳**：变更执行时间
    
- **@tag**：涉及的结构体标识符
    
- **发起模块**：谁发起的修改请求
    
- **变更类型**：类型变更/新增字段/删除字段/默认值变更/字段废弃/回滚
    
- **变更内容**：具体改了什么
    
- **受影响模块**：哪些模块被同步更新
    
- **执行状态**：已完成/冲突待解决/已回滚
    
- **变更前后对比**：改之前是什么样，改之后是什么样
    
- **关联版本**：变更后的 `@version` 版本号
    

### 9.3 示例

markdown

# 公共管理链表 — 结构体变更历史
## 2026-05-27 14:30 | @tag: MSG_STATE_GlobalState_OUT | @version: 1.1
- **发起模块**: key_module
- **变更类型**: 类型变更
- **变更内容**: head_power 由 uint8_t 改为 uint16_t
- **受影响模块**: state_module, display_module, timer_module
- **执行状态**: ✅ 已完成
- **变更前**: `uint8_t head_power[4]`
- **变更后**: `uint16_t head_power[4]`
## 2026-05-27 15:10 | @tag: MSG_TIMER_TimerParams | @version: 2.0
- **发起模块**: timer_module
- **变更类型**: 新增字段
- **变更内容**: 增加 timer_max_value 字段
- **受影响模块**: state_module
- **执行状态**: ✅ 已完成
## 2026-05-27 16:00 | @tag: MSG_STATE_GlobalState_OUT | @version: 1.0
- **发起模块**: display_module
- **变更类型**: 回滚
- **变更内容**: 回滚 head_power 到 uint8_t（版本 1.0）
- **受影响模块**: state_module, display_module, timer_module
- **执行状态**: ✅ 已完成

## 十、可操作性评估

### 10.1 核心能力（可以落地）

|能力|实现难度|说明|
|---|---|---|
|注释标记解析|低|正则匹配 `@tag`、`@import` 等关键字，成熟的文本处理|
|结构体字段提取|低|用正则或轻量C解析器提取 `typedef struct` 中的字段名和类型|
|副本生成|低|根据 `@import` 列表从源结构体裁剪字段，生成目标代码|
|指令清空|低|删除 `/** ... */` 中包含特定标签的注释块|
|链表归档|低|追加一条记录到 `.md` 或 `.json` 文件|
|一致性校验|中|逐字段对比链表记录和实际 `.h` 文件，生成差异报告|
|方向校验|中|检查 `_IN`/`_OUT` 后缀与 `@import` 方向是否合规|
|冲突检测|中|检查两个模块对同一字段的不同修改请求|
|回滚操作|中|从链表历史中读取目标版本的字段定义，逆向替换|

**总体评价**：核心功能全部可以用 Python 标准库实现，不需要第三方依赖。注释解析用正则，代码操作用文本替换，链表归档用 JSON 或 Markdown 追加。工程上是可行的。

### 10.2 已识别的问题与风险

|风险|说明|应对|
|---|---|---|
|**注释格式不规范**|AI 或开发人员写的 `@import` 格式错误|解析器严格校验格式，不规范时报错并给出正确格式示例|
|**@import 与 @reserve 冲突**|同一字段同时出现在两个列表中|管理工具检测到冲突时报错，列出冲突字段名|
|**@modify 并发冲突**|两个模块对同一字段发起不同修改|生成冲突报告，暂停执行，等待人工裁决|
|**指令清空原子性**|清空指令时部分文件写入失败|先备份所有待修改文件，失败时回滚|
|**@import 循环依赖**|A 模块 @import B，B 模块 @import A|构建依赖图，检测循环时报警但不自动中止|
|**结构体定义与 @import 不同步**|手动修改结构体但未更新 @import|由一致性校验机制兜底|
|**管理工具版本兼容性**|旧版工具遇到新标签|扫描到无法识别的标签时报警并跳过|
|**Git 合并冲突**|两个分支修改了同一个 @import|冲突时人工解决，工具提供 `--check` 验证|

### 10.3 与 AI 行为规范的配合

这套管理工具的操作生命周期和报错机制，和之前定下的 AI 行为规范完全一致：

- **Don't assume. Ask.**：前置条件不满足时，工具不猜测用户意图，直接报错并列出可用选项
    
- **Define success criteria.**：每个操作类型都有明确的成功条件，不满足则不执行
    
- **如果存在多种理解方式，全部列出**：报错信息中列出可用的字段、可用的版本号
    

## 十一、与现有机制的完整配合

|机制|职责|层次|
|---|---|---|
|**无保护的 `.h` 文件**|编译器强制隔离，违规即报错|编译期|
|**`__weak` 直调**|模块间通信，零编译依赖|链接期|
|**后缀方向规范**|`_IN`/`_OUT` 后缀规范数据流向，管理工具自动校验|设计期|
|**标记语言**|结构体的“使用说明书”，AI读懂并自动生成代码|设计期|
|**管理工具（Python）**|解析标记语言，自动生成声明、校验一致性、校验方向、执行修改|维护期|
|**操作生命周期**|严格的操作顺序和前置条件检查，类似编译报错|维护期|
|**一致性校验**|链表 ↔ 代码同步检查，不一致则暂停|维护期|
|**`interface_map.h`**|通信契约文档，AI维护|维护期|
|**公共管理链表**|变更历史归档，可追溯|维护期|
|**公共命名规范**|常量/结构体统一命名，管理工具自动识别类型和方向|设计期|

## 十二、文档更新记录

|版本|日期|更新内容|
|---|---|---|
|V1.0|2026-05-27|创建初始版本，包含标记语言语法、管理工具设计、命名规范、编译期隔离机制、漏洞分析|
|V1.1|2026-05-27|补充头文件预处理铁律（注释掉的 `#ifndef` 保护）|
|V2.0|2026-05-27|补充一致性校验机制、@deprecate/@version/@rollback 标签、指令生命周期、自动修复能力、多标签支持、后缀驱动调用方向规范、操作生命周期与报错机制、可操作性评估|

本回答由 AI 生成，内容仅供参考，请仔细甄别。

## 一、背景与目标

### 1.1 背景

在 `__weak` 直调架构下，跨模块通信已实现编译期零耦合——模块间不通过 `#include` 共享结构体定义，而是通过 `__weak` 回调传递数据。但这也带来了新问题：

- **同结构体多副本一致性**：同一个结构体在多个模块各有一份独立声明，如何保证一致？
    
- **字段按需裁剪**：每个模块只需要结构体的部分字段，如何声明“我要哪些、不要哪些”？
    
- **变更溯源**：谁在什么时候改了哪个结构体的哪个字段？为什么改？
    

### 1.2 目标

用一套**基于注释的轻量级标记语言**，让 AI 和 Python 管理工具能读懂每个结构体的“使用说明书”，自动完成结构体多副本一致性维护、字段按需裁剪、变更历史归档。

### 1.3 核心原则

- **指令即注释**：所有管理指令都写在 `.h` 文件的注释中，不侵入代码逻辑
    
- **按需导入**：每个模块只声明自己需要的字段，其他字段用默认值跳过
    
- **自动清空**：Python 工具执行完指令后，自动删除指令注释块
    
- **集中归档**：所有变更记录归档到公共维护文件，形成管理链表
    
- **编译期强制隔离**：无保护的 `.h` 文件只能被一个模块引用，违规即报错
    
- **修改前先校验**：每次执行修改前，强制校验链表与代码一致性
    
- **后缀驱动调用方向**：`_IN`/`_OUT` 后缀规范数据流向，管理工具自动校验
    

## 二、编译期强制隔离：无保护的头文件

### 2.1 设计原理

传统的 `.h` 文件使用 `#ifndef` / `#define` / `#endif` 保护，允许多个 `.c` 文件安全地 `#include` 同一个 `.h`，共享其中的结构体定义。但这也导致了编译期耦合——所有模块都可以自由引用同一个公共结构体。

本方案的做法是：**保留 `#ifndef` 保护符的书写，但将其注释掉。** 这样，编译器不会看到保护符，每个 `.h` 文件只能被一个 `.c` 文件 `#include`。如果第二个模块也尝试 `#include` 同一个 `.h`，链接器会报“重复定义”错误。

### 2.2 代码示例

**传统写法（允许多模块共享）：**

c

#ifndef _GLOBAL_STATE_H_
#define _GLOBAL_STATE_H_
typedef struct {
    uint8_t head_power[4];
} MSG_STATE_GlobalState_OUT;
#endif

**本方案写法（编译期强制隔离）：**

c

// #ifndef _GLOBAL_STATE_H_    ← 保留但注释掉
// #define _GLOBAL_STATE_H_    ← 保留但注释掉
/**
 * @tag MSG_STATE_GlobalState_OUT
 * @message 状态机模块输出的全局状态结构体
 * @create state_module
 * @import display_module MSG_STATE_GlobalState_OUT  head_power[4]
 */
typedef struct {
    uint8_t head_power[4];
} MSG_STATE_GlobalState_OUT;
// #endif  ← 保留但注释掉

### 2.3 效果

- **唯一引用**：只有创建该结构体的主模块可以 `#include` 这个 `.h` 文件
    
- **违规即报错**：如果其他模块也尝试 `#include`，链接器立即报“重复定义”错误
    
- **强制走__weak**：所有跨模块数据访问必须通过 `__weak` 回调，从机制上堵死直接 `#include` 的路
    
- **一眼可查**：注释掉的 `#ifndef` 本身就是醒目的视觉标记，提醒开发者“这个文件有特殊规则”
    

## 三、标记语言完整语法

### 3.1 标签一览

|标签|功能|位置|示例|
|---|---|---|---|
|`@body`|文件总体描述|文件头部|`@body 本文件定义全局状态结构体`|
|`@tag`|唯一标识符|每个声明块|`@tag MSG_STATE_GlobalState_OUT`|
|`@message`|简要说明|每个声明块|`@message 状态机模块输出的全局状态`|
|`@create`|在指定模块创建副本|源结构体定义处|`@create state_module`|
|`@import`|指定模块导入哪些字段|源结构体定义处|`@import display_module MSG_STATE_GlobalState_OUT head_power[4]`|
|`@modify`|发起修改请求|任意模块文件|`@modify MSG_STATE_GlobalState_OUT`|
|`@change`|修改请求的具体变更|任意模块文件|`@change head_power type=uint8_t→uint16_t`|
|`@scope`|修改请求的发起模块|任意模块文件|`@scope key_module`|
|`@affected`|修改请求影响的其他模块|任意模块文件|`@affected display_module`|
|`@reserve`|预留/跳过字段|源结构体定义处|`@reserve MSG_STATE_GlobalState_OUT reserved_1, reserved_2`|
|`@deprecate`|标记字段即将废弃|源结构体定义处|`@deprecate old_field replace=new_field deadline=2026-06-30`|
|`@version`|结构体版本号|源结构体定义处|`@version 2.1`|
|`@rollback`|回滚到指定版本|任意模块文件|`@rollback MSG_STATE_GlobalState_OUT to=1.0`|

### 3.2 标签详细语法

#### @body（文件总体描述）

text

@body <描述文本>

每个 `.h` 文件只能有一个 `@body`，放在文件最顶部的注释中。

#### @tag（唯一标识符）

text

@tag <标识符>

推荐使用完整的命名规范标识符作为标签值，如 `MSG_STATE_GlobalState_OUT`。每个声明块可以有多个 `@tag`，每个标签定义一段独立的声明内容。

#### @message（简要说明）

text

@message <说明文本>

#### @create（创建副本）

text

@create <模块名>                    // 创建完整副本
@create <模块名> partial=true       // 创建部分副本（与@import配合）

#### @import（按需导入字段）

text

@import <模块名> <结构体名> <字段1>, <字段2>, ...
@import display_module MSG_STATE_GlobalState_OUT head_power[4], work_mode

每个模块可以有多条 `@import` 指令，精确到结构体内的具体字段。不需要的字段自动跳过，用预留默认值填充。

#### @modify（发起修改请求）

text

@modify <结构体@tag标识符>
@message <修改原因说明>
@change <字段名> type=<旧类型>→<新类型>
@change <字段名> add=<类型> default=<默认值>
@change <字段名> remove=true
@change <字段名> default=<新默认值>
@scope <发起修改的模块>
@affected <受影响模块1>, <受影响模块2>

任何模块都可以发起对公共结构体的修改请求，不需要跳转到源定义文件中去改。

#### @reserve（预留字段）

text

@reserve <结构体名> <字段1>, <字段2>, ...

预留字段用默认值填充，管理工具保证 `@import` 字段不能出现在 `@reserve` 列表中。

#### @deprecate（标记字段废弃）

text

@deprecate <字段名> replace=<替代字段> deadline=<废弃日期>
@deprecate old_power_level replace=head_power[0] deadline=2026-06-30

#### @version（结构体版本号）

text

@version <版本号>

每次 `@modify` 执行后，版本号自动递增。

#### @rollback（回滚指令）

text

@rollback <结构体@tag标识符> to=<目标版本>
@rollback MSG_STATE_GlobalState_OUT to=1.0

管理工具自动逆向操作，将结构体恢复到指定版本。

### 3.3 完整注释块示例

c

// #ifndef _GLOBAL_STATE_H_
// #define _GLOBAL_STATE_H_
/**
 * @body
 * 本文件定义电磁炉控制系统的全局状态结构体。
 * 该结构体由 state_module 创建和维护，其他模块通过 __weak 回调获取。
 *
 * @tag MSG_STATE_GlobalState_OUT
 * @message 状态机模块输出的全局状态结构体，包含当前工作模式、功率档位、定时器状态等
 * @version 1.2
 *
 * @create state_module
 * @create display_module partial=true
 * @create timer_module   partial=true
 *
 * @import state_module   MSG_STATE_GlobalState_OUT  head_power[4], timer_remaining, work_mode
 * @import display_module MSG_STATE_GlobalState_OUT  seg_chars[8], led_state, blink_phase
 * @import timer_module   MSG_STATE_GlobalState_OUT  timer_remaining, timer_active
 *
 * @reserve MSG_STATE_GlobalState_OUT  reserved_1, reserved_2
 *
 * @deprecate old_power_level replace=head_power[0] deadline=2026-06-30
 */
typedef struct {
    uint8_t head_power[4];      // 4个炉头的功率档位
    uint8_t work_mode;          // 当前工作模式
    uint16_t timer_remaining;   // 定时剩余时间
    uint8_t timer_active;       // 定时是否激活
    uint8_t seg_chars[8];       // 数码管显示内容
    uint8_t led_state;          // LED灯状态位掩码
    uint8_t blink_phase;        // 闪烁相位
    uint8_t reserved_1;         // 预留字段
    uint16_t reserved_2;        // 预留字段
    uint8_t old_power_level;    // @deprecate: 已废弃，请用 head_power[0]
} MSG_STATE_GlobalState_OUT;
// #endif

### 3.4 修改请求示例

c

// 在 key_module.c 中发起修改请求
/**
 * @modify MSG_STATE_GlobalState_OUT
 * @message key_module 需要将 head_power 改为 uint16_t 以支持更高功率档位
 * @change head_power  type=uint8_t→uint16_t
 * @scope key_module
 * @affected state_module, display_module
 */

## 四、后缀驱动的调用方向规范

### 4.1 核心规则

|后缀|含义|谁创建|谁可以使用|调用方向|
|---|---|---|---|---|
|**（无后缀）**|本模块内部使用|本模块|**仅本模块**|不跨模块|
|`_IN`|本模块接收的输入|本模块|发送方模块|其他模块 → 本模块|
|`_OUT`|本模块发出的输出|本模块|接收方模块|本模块 → 其他模块|

### 4.2 调用方向铁律

text

上层模块（APP层）
    │
    ├─ 可以调用下层模块的 _OUT 结构体（下层输出给上层）
    │
    └─ 禁止调用下层模块的无后缀结构体（那是下层内部用的）
       禁止下层模块直接调用上层模块的 _OUT 结构体（只能上调下，不能下调上）

**"只能上调下，不能下调上"的规范**：

- 下层模块（如驱动层）的输出结构体带 `_OUT` 后缀，供上层模块（如APP层）读取
    
- 上层模块的输出结构体也带 `_OUT` 后缀，但下层模块不能直接引用——下层只能通过 `__weak` 回调接收上层指令，不能直接 `#include` 上层的结构体定义
    
- 无后缀的结构体是本模块的“私有财产”，任何其他模块都不能引用
    

### 4.3 与无保护头文件的配合

无保护的 `.h` 文件只有本模块能 `#include`，所以：

- **本模块的无后缀结构体**：自然只能被本模块使用，其他模块无法 `#include`
    
- **本模块的 `_IN` 结构体**：由本模块定义，发送方模块通过 `__weak` 回调的参数类型来引用
    
- **本模块的 `_OUT` 结构体**：由本模块定义，接收方模块通过 `__weak` 回调的参数类型来引用
    

### 4.4 管理工具的自动校验

管理工具扫描所有 `@import` 声明，自动校验调用方向：

|校验项|规则|违规时的报错|
|---|---|---|
|**私有结构体外泄**|无后缀的结构体不能出现在任何其他模块的 `@import` 中|`错误: [MSG_STATE_InternalData] 是无后缀的私有结构体，不能在其他模块中 @import。`|
|**上调下**|上层模块可以 `@import` 下层模块的 `_OUT` 结构体|合法，不报错|
|**下调上**|下层模块不能 `@import` 上层模块的 `_OUT` 结构体|`错误: [MSG_APP_Config_OUT] 是上层模块的输出结构体，下层模块不能直接引用。请通过 __weak 回调获取。`|
|**输入方向**|`_IN` 结构体只能被发送方模块 `@import`|`错误: [MSG_KEY_KeyEvent_IN] 是 key_module 的输入结构体，只有发送按键事件的模块才能 @import。`|

### 4.5 完整示例

c

// ===== display_module 的 .h 文件（无保护）=====
// #ifndef _DISPLAY_DATA_H_
// #define _DISPLAY_DATA_H_
/**
 * @tag MSG_DISPLAY_InternalBuffer
 * @message 显示模块内部缓冲区，仅供本模块使用
 * （无后缀 = 私有，其他模块不能 @import）
 */
typedef struct {
    uint8_t seg_buff[8];
    uint8_t led_buff;
} MSG_DISPLAY_InternalBuffer;
/**
 * @tag MSG_DISPLAY_DisplayData_IN
 * @message 显示模块接收的输入结构体
 * @create display_module
 * @import state_module MSG_DISPLAY_DisplayData_IN  seg_chars[8], led_state
 */
typedef struct {
    uint8_t seg_chars[8];
    uint8_t led_state;
} MSG_DISPLAY_DisplayData_IN;
/**
 * @tag MSG_DISPLAY_Status_OUT
 * @message 显示模块输出的状态结构体，供上层模块读取
 * @create display_module
 * @import state_module MSG_DISPLAY_Status_OUT  blink_phase
 */
typedef struct {
    uint8_t blink_phase;
} MSG_DISPLAY_Status_OUT;
// #endif

## 五、公共命名规范

### 5.1 命名格式

|类型|格式|示例|
|---|---|---|
|**常量/宏定义**|`IDX_<模块名>_<变量名>`|`IDX_STATE_work_mode_STANDBY`|
|**结构体（通用）**|`MSG_<模块名>_<变量名>`|`MSG_STATE_GlobalState`|
|**结构体（输入）**|`MSG_<模块名>_<变量名>_IN`|`MSG_KEY_KeyEvent_IN`|
|**结构体（输出）**|`MSG_<模块名>_<变量名>_OUT`|`MSG_STATE_DisplayData_OUT`|

### 5.2 前缀含义

|前缀|含义|适用场景|
|---|---|---|
|`IDX_`|常量/索引/枚举值|功率档位定义、超时阈值、状态枚举|
|`MSG_`|消息/结构体|跨模块传递的数据结构|

### 5.3 后缀含义

|后缀|含义|说明|
|---|---|---|
|`_IN`|输入|其他模块发送给本模块的数据|
|`_OUT`|输出|本模块发送给其他模块的数据|
|（无后缀）|通用/共享|模块内部使用或双向共享|

### 5.4 模块名列表

|模块名|对应模块|说明|
|---|---|---|
|`STATE`|state_module|状态机模块|
|`KEY`|key_module|按键模块|
|`DISPLAY`|display_module|显示模块|
|`TIMER`|timer_module|定时器模块|

## 六、管理工具（Python程序）

### 6.1 工具职责

Python 管理工具 `struct_manager.py` 负责以下工作：

1. **扫描**：扫描所有模块文件，收集 `@import`、`@modify`、`@create`、`@reserve` 等指令
    
2. **校验**：检查冲突——两个模块对同一字段发起不同修改时报警
    
3. **一致性检查**：校验公共管理链表与实际 `.h` 文件声明是否完全一致
    
4. **方向检查**：校验 `_IN`/`_OUT` 后缀的调用方向是否合规
    
5. **执行**：自动修改源结构体定义，同步更新所有受影响模块的副本
    
6. **清空**：执行完毕后，自动删除所有指令注释块
    
7. **归档**：在公共维护文件中生成变更记录，形成管理链表
    

### 6.2 执行流程

text

扫描所有 .c/.h 文件
    ↓
收集所有 @import、@modify、@create、@reserve、@deprecate 声明
    ↓
一致性校验：公共管理链表 ↔ .h 文件声明
    ↓ 不通过 → 生成差异报告 → 暂停 → 等待人工确认
    ↓ 通过
方向校验：_IN/_OUT 后缀调用方向
    ↓ 不通过 → 生成违规报告 → 暂停 → 等待人工修正
    ↓ 通过
前置条件检查：@create 先于 @import，@import 先于 @modify
    ↓ 不通过 → 生成报错报告 → 暂停
    ↓ 通过
校验冲突：
    - 两个模块对同一字段发起不同修改 → 报错
    - 修改字段名不存在 → 报错
    - 类型变更与现有 @import 不兼容 → 警告
    - @import 字段出现在 @reserve 列表中 → 报错
    ↓
无冲突 → 自动执行修改：
    1. 修改源结构体定义
    2. 同步更新所有受影响模块的副本
    3. 更新 interface_map.h
    4. 更新 @version 版本号
    5. 重新编译验证
    ↓
有冲突 → 生成冲突报告，等待人工裁决
    ↓
清空所有指令注释块（@import、@modify、@create、@reserve、@deprecate、@rollback）
    ↓
在公共管理链表中追加变更记录
    ↓
再次运行一致性校验（确认修改后一致）

### 6.3 指令生命周期

text

写入 → 扫描 → 校验（一致性+方向+前置条件+冲突）→ 执行 → 清空 → 归档 → 再校验

指令只在需要修改时才存在，修改完成后自动消失。代码始终保持干净。

### 6.4 运行模式

bash

# 干运行模式（只检查，不修改）
python struct_manager.py --check
# 执行模式（检查通过后执行修改）
python struct_manager.py --apply
# 自动修复模式（仅限安全修复）
python struct_manager.py --check --fix
# 增量扫描模式（只扫描有变更的文件）
python struct_manager.py --apply --incremental

## 七、操作生命周期与报错机制

### 7.1 操作生命周期

管理工具中的所有操作必须遵循严格的生命周期顺序。违反顺序时，工具必须报错并中止。

text

创建（@create）
    ↓
声明（@import）
    ↓
修改（@modify）
    ↓
废弃（@deprecate）
    ↓
回滚（@rollback）

### 7.2 前置条件检查

每个操作类型都有对应的前置条件。前置条件不满足时，工具必须报错并中止。

|操作|前置条件|违反时的报错|
|---|---|---|
|`@import`|目标结构体必须已被 `@create`|`错误: 结构体 [MSG_XXX] 尚未创建，无法导入。请先执行 @create。`|
|`@modify`|目标结构体必须已被 `@create`|`错误: 结构体 [MSG_XXX] 尚未创建，无法修改。请先执行 @create。`|
|`@modify` 字段|目标字段必须存在于结构体中|`错误: 字段 [field_name] 在结构体 [MSG_XXX] 中不存在。可用字段: [field1, field2, ...]`|
|`@modify` 类型变更|新类型必须与现有 `@import` 兼容|`警告: 字段 [field_name] 类型变更影响模块 [module_name]，该模块 @import 了此字段。建议同步更新。`|
|`@deprecate`|目标字段必须存在于结构体中|`错误: 字段 [field_name] 在结构体 [MSG_XXX] 中不存在，无法标记为废弃。`|
|`@deprecate`|必须提供替代字段或废弃原因|`警告: @deprecate 建议提供 replace= 参数指明替代方案。`|
|`@rollback`|目标版本必须存在于公共管理链表中|`错误: 版本 [1.0] 在结构体 [MSG_XXX] 的变更历史中不存在。可用版本: [1.0, 1.1, 1.2]`|
|`@rollback`|回滚操作不能有冲突的并发修改|`错误: 结构体 [MSG_XXX] 在回滚目标版本之后有未归档的修改，请先处理冲突。`|
|`@import` 字段|字段不能出现在 `@reserve` 列表中|`错误: 字段 [field_name] 已被 @reserve 标记为预留字段，无法 @import。`|
|`@create`|同一结构体不能重复创建|`错误: 结构体 [MSG_XXX] 已在模块 [module_name] 中创建，不能重复创建。`|

### 7.3 报错级别

|级别|说明|工具行为|
|---|---|---|
|**ERROR**|前置条件不满足，操作无法执行|中止当前操作，跳过后续操作|
|**WARNING**|可能存在问题，但操作可以继续|输出警告信息，继续执行|
|**INFO**|提示信息|输出提示，继续执行|

### 7.4 报错输出格式

text

========================================
  struct_manager.py — 执行报告
  时间: 2026-05-27 17:30
  模式: --apply
========================================
ERROR (3):
  [MSG_NONEXISTENT] 结构体尚未创建，无法导入。请先执行 @create。
    位置: display_module.c, 第 15 行
  [MSG_NONEXISTENT] 结构体尚未创建，无法修改。请先执行 @create。
    位置: key_module.c, 第 22 行
  [reserved_1] 已被 @reserve 标记为预留字段，无法 @import。
    位置: display_module.c, 第 18 行
WARNING (1):
  [head_power] 类型变更影响模块 [display_module]，该模块 @import 了此字段。建议同步更新。
    位置: key_module.c, 第 25 行
========================================
  执行结论: 失败 (3 个错误)
  已中止。请修复上述错误后重试。
========================================

## 八、一致性校验机制

### 8.1 校验目标

管理工具在执行任何修改前，必须先校验**公共管理链表**与**实际 `.h` 文件中的声明**是否完全一致。不一致时暂停所有操作，生成差异报告，等待人工确认。

### 8.2 校验时机

|时机|说明|
|---|---|
|**每次执行修改前**|强制校验，不一致则中止|
|**Git 提交前**（预提交钩子）|自动触发校验，不一致则阻止提交|
|**定期巡检**（AI 定时任务）|每日自动扫描，发现不一致立即报警|

### 8.3 校验内容

|校验项|说明|不一致时的处理|
|---|---|---|
|**链表记录 ↔ .h 文件存在性**|链表中每个 `@tag` 是否在某个 `.h` 文件中有对应定义|标记为“孤立记录”|
|**.h 文件 ↔ 链表记录**|每个带 `@tag` 的结构体是否在链表中有对应记录|标记为“未登记结构体”|
|**字段定义一致性**|链表记录的字段名、类型、顺序是否与 `.h` 文件完全一致|逐字段对比，生成差异报告|
|**@import 关系一致性**|各模块中管理工具生成的结构体副本，是否与 `@import` 声明一致|标记为“副本不同步”|
|**@version 版本号一致性**|链表记录的版本号是否与 `.h` 文件中的 `@version` 一致|标记为“版本不一致”|
|**调用方向一致性**|`_IN`/`_OUT` 后缀的使用是否与 `@import` 方向一致|标记为“方向违规”|

### 8.4 差异报告示例

text

========================================
  一致性校验报告 — 2026-05-27 16:30
========================================
✅ 链表记录 ↔ .h 文件: 12/12 匹配
✅ .h 文件 ↔ 链表记录: 4/4 已登记
✅ @version 版本号: 4/4 一致
✅ 调用方向: 8/8 合规
❌ 字段定义不一致 (1处):
  @tag: MSG_STATE_GlobalState_OUT
  字段: head_power
  链表记录: uint16_t head_power[4]
  实际代码: uint8_t head_power[4]
  建议: 更新链表记录或回滚代码修改
⚠️ 孤立记录 (1处):
  @tag: MSG_TIMER_OldParams
  链表中有记录，但代码中未找到对应定义
  建议: 确认该结构体是否已删除，若是则归档该记录
========================================
  校验结论: 不通过 (1处不一致, 1处孤立)
  管理工具已暂停，请处理上述问题后重试。
========================================

### 8.5 自动修复能力

对于可自动修复的不一致，管理工具提供 `--fix` 选项：

**可自动修复：**

- 链表记录的格式错误（如缩进、换行符不一致）
    
- 时间戳格式标准化
    
- 字段顺序调整（以 `.h` 文件为准，自动更新链表记录）
    

**必须人工确认：**

- 字段类型不一致
    
- 字段名称不一致
    
- 链表中有记录但代码中找不到对应结构体
    
- 代码中有结构体但链表无记录
    
- 版本号不一致
    
- 调用方向违规
    

### 8.6 与 AI 的配合

AI 每日定时运行 `--check` 模式，发现不一致时：

1. **自动生成修复建议**：AI 读取差异报告，判断不一致的原因
    
2. **简单问题自动修复**：如字段顺序调整，AI 自行运行 `--fix`
    
3. **复杂问题上报**：如字段类型冲突，AI 生成详细报告，通知人工裁决
    
4. **修复后自动校验**：AI 修复后重新运行 `--check`，确认通过后记录日志
    

## 九、公共管理链表

### 9.1 存放位置与格式

管理链表存放在 `interface_map.md` 或 `interface_map.json` 中，与 `interface_map.h` 放在同一目录。

### 9.2 记录内容

每条变更记录包含：

- **时间戳**：变更执行时间
    
- **@tag**：涉及的结构体标识符
    
- **发起模块**：谁发起的修改请求
    
- **变更类型**：类型变更/新增字段/删除字段/默认值变更/字段废弃/回滚
    
- **变更内容**：具体改了什么
    
- **受影响模块**：哪些模块被同步更新
    
- **执行状态**：已完成/冲突待解决/已回滚
    
- **变更前后对比**：改之前是什么样，改之后是什么样
    
- **关联版本**：变更后的 `@version` 版本号
    

### 9.3 示例

markdown

# 公共管理链表 — 结构体变更历史
## 2026-05-27 14:30 | @tag: MSG_STATE_GlobalState_OUT | @version: 1.1
- **发起模块**: key_module
- **变更类型**: 类型变更
- **变更内容**: head_power 由 uint8_t 改为 uint16_t
- **受影响模块**: state_module, display_module, timer_module
- **执行状态**: ✅ 已完成
- **变更前**: `uint8_t head_power[4]`
- **变更后**: `uint16_t head_power[4]`
## 2026-05-27 15:10 | @tag: MSG_TIMER_TimerParams | @version: 2.0
- **发起模块**: timer_module
- **变更类型**: 新增字段
- **变更内容**: 增加 timer_max_value 字段
- **受影响模块**: state_module
- **执行状态**: ✅ 已完成
## 2026-05-27 16:00 | @tag: MSG_STATE_GlobalState_OUT | @version: 1.0
- **发起模块**: display_module
- **变更类型**: 回滚
- **变更内容**: 回滚 head_power 到 uint8_t（版本 1.0）
- **受影响模块**: state_module, display_module, timer_module
- **执行状态**: ✅ 已完成

## 十、可操作性评估

### 10.1 核心能力（可以落地）

|能力|实现难度|说明|
|---|---|---|
|注释标记解析|低|正则匹配 `@tag`、`@import` 等关键字，成熟的文本处理|
|结构体字段提取|低|用正则或轻量C解析器提取 `typedef struct` 中的字段名和类型|
|副本生成|低|根据 `@import` 列表从源结构体裁剪字段，生成目标代码|
|指令清空|低|删除 `/** ... */` 中包含特定标签的注释块|
|链表归档|低|追加一条记录到 `.md` 或 `.json` 文件|
|一致性校验|中|逐字段对比链表记录和实际 `.h` 文件，生成差异报告|
|方向校验|中|检查 `_IN`/`_OUT` 后缀与 `@import` 方向是否合规|
|冲突检测|中|检查两个模块对同一字段的不同修改请求|
|回滚操作|中|从链表历史中读取目标版本的字段定义，逆向替换|

**总体评价**：核心功能全部可以用 Python 标准库实现，不需要第三方依赖。注释解析用正则，代码操作用文本替换，链表归档用 JSON 或 Markdown 追加。工程上是可行的。

### 10.2 已识别的问题与风险

|风险|说明|应对|
|---|---|---|
|**注释格式不规范**|AI 或开发人员写的 `@import` 格式错误|解析器严格校验格式，不规范时报错并给出正确格式示例|
|**@import 与 @reserve 冲突**|同一字段同时出现在两个列表中|管理工具检测到冲突时报错，列出冲突字段名|
|**@modify 并发冲突**|两个模块对同一字段发起不同修改|生成冲突报告，暂停执行，等待人工裁决|
|**指令清空原子性**|清空指令时部分文件写入失败|先备份所有待修改文件，失败时回滚|
|**@import 循环依赖**|A 模块 @import B，B 模块 @import A|构建依赖图，检测循环时报警但不自动中止|
|**结构体定义与 @import 不同步**|手动修改结构体但未更新 @import|由一致性校验机制兜底|
|**管理工具版本兼容性**|旧版工具遇到新标签|扫描到无法识别的标签时报警并跳过|
|**Git 合并冲突**|两个分支修改了同一个 @import|冲突时人工解决，工具提供 `--check` 验证|

### 10.3 与 AI 行为规范的配合

这套管理工具的操作生命周期和报错机制，和之前定下的 AI 行为规范完全一致：

- **Don't assume. Ask.**：前置条件不满足时，工具不猜测用户意图，直接报错并列出可用选项
    
- **Define success criteria.**：每个操作类型都有明确的成功条件，不满足则不执行
    
- **如果存在多种理解方式，全部列出**：报错信息中列出可用的字段、可用的版本号
    

## 十一、与现有机制的完整配合

|机制|职责|层次|
|---|---|---|
|**无保护的 `.h` 文件**|编译器强制隔离，违规即报错|编译期|
|**`__weak` 直调**|模块间通信，零编译依赖|链接期|
|**后缀方向规范**|`_IN`/`_OUT` 后缀规范数据流向，管理工具自动校验|设计期|
|**标记语言**|结构体的“使用说明书”，AI读懂并自动生成代码|设计期|
|**管理工具（Python）**|解析标记语言，自动生成声明、校验一致性、校验方向、执行修改|维护期|
|**操作生命周期**|严格的操作顺序和前置条件检查，类似编译报错|维护期|
|**一致性校验**|链表 ↔ 代码同步检查，不一致则暂停|维护期|
|**`interface_map.h`**|通信契约文档，AI维护|维护期|
|**公共管理链表**|变更历史归档，可追溯|维护期|
|**公共命名规范**|常量/结构体统一命名，管理工具自动识别类型和方向|设计期|

## 十二、文档更新记录

|版本|日期|更新内容|
|---|---|---|
|V1.0|2026-05-27|创建初始版本，包含标记语言语法、管理工具设计、命名规范、编译期隔离机制、漏洞分析|
|V1.1|2026-05-27|补充头文件预处理铁律（注释掉的 `#ifndef` 保护）|
|V2.0|2026-05-27|补充一致性校验机制、@deprecate/@version/@rollback 标签、指令生命周期、自动修复能力、多标签支持、后缀驱动调用方向规范、操作生命周期与报错机制、可操作性评估|

本回答由 AI 生成，内容仅供参考，请仔细甄别。