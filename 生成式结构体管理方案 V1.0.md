# 生成式结构体管理方案 V1.0

## 一、设计命题

### 1.1 原方案的根本问题

"注释驱动的结构体管理标记语言 V2.0" 的设计模式是**注释→扫描→解析→执行**：

```
人/AI 在各模块 .h 写 @import/@create/@modify 标签
        ↓
struct_manager.py 扫描所有文件收集标签
        ↓
解析标签语义，校验冲突
        ↓
自动修改源结构体 + 同步副本 + 清空标签 + 归档
```

这套流程的**执行保真度依赖标签的正确写入**。当操作者从"你"换成"另一个 AI"时：

- 新 AI 不知道应该写 `@import` 还是 `@create`
- 新 AI 可能忘写 `@affected` 导致副本不同步
- 新 AI 可能写错字段名，工具报错但不修代码
- 所有一致性问题的根因从"结构体字段不同步"变成了"标签写错"

**标签只是把问题移到了上游，没有消除。**

### 1.2 生成式方案的核心差异

```
人/AI 编辑 structs.json (单一数据源)
        ↓
generate_structs.py 读 JSON → 生成所有模块的结构体 .h
        ↓
check_structs.py --check → 对比生成输出与实际文件
        ↓
不一致 → 提交被阻断 → 强制重新生成
```

**关键**：操作者只需要编辑 JSON。生成的 `.h` 文件带 `AUTO-GENERATED` 标记。验证方式是"重新生成后对比"——不依赖任何标签、注释、或操作者的自觉。

---

## 二、structs.json — 单一数据源

### 2.1 完整 Schema

```json
{
  "version": "1.0",
  "serial": 1,
  "structs": {
    "<StructKey>": {
      "description": "<人类可读说明>",
      "owner": "<拥有该结构体的模块名>",
      "suffix": "OUT | IN | (空字符串)",
      "fields": [
        {
          "name": "<字段名>",
          "type": "<C类型>",
          "count": "<数组长度, 非数组省略>",
          "note": "<字段说明, 可选>"
        }
      ],
      "consumers": {
        "<模块名>": {
          "fields": ["<字段1>", "<字段2>"],
          "struct_name": "<该模块使用的结构体名, 可选, 默认自动生成>"
        }
      },
      "deprecated": {
        "<字段名>": {
          "replaced_by": "<替代字段>",
          "deadline": "<YYYY-MM-DD>",
          "note": "<废弃原因, 可选>"
        }
      }
    }
  }
}
```

### 2.2 字段说明

| 字段 | 必须 | 类型 | 说明 |
|------|------|------|------|
| `version` | 是 | string | Schema 版本号，当前固定 `"1.0"` |
| `serial` | 是 | int | 单调递增，每次编辑 +1。生成文件内嵌此值 |
| `structs` | 是 | object | 所有跨模块结构体，key 为结构体标识符 |
| `structs.{key}.description` | 是 | string | 结构体用途说明 |
| `structs.{key}.owner` | 是 | string | 拥有该结构体的模块（创建和维护） |
| `structs.{key}.suffix` | 是 | enum | `"OUT"` = 本模块产出, `"IN"` = 本模块接收, `""` = 内部私有 |
| `structs.{key}.fields` | 是 | array | 字段列表，顺序即内存布局顺序 |
| `structs.{key}.fields[].name` | 是 | string | 字段名 (snake_case) |
| `structs.{key}.fields[].type` | 是 | string | C 类型，如 `uint8_t`, `uint16_t`, `int8_t` |
| `structs.{key}.fields[].count` | 否 | int | 数组长度。省略 = 非数组 |
| `structs.{key}.fields[].note` | 否 | string | 字段说明 |
| `structs.{key}.consumers` | 否 | object | 消费此结构体的模块列表 |
| `structs.{key}.consumers.{module}.fields` | 是 | array | 该模块需要的字段名列表 |
| `structs.{key}.consumers.{module}.struct_name` | 否 | string | 自定义结构体名，默认 `{Module}_{StructKey}_IN_t` |
| `structs.{key}.deprecated` | 否 | object | 已废弃的字段 |

### 2.3 完整示例

```json
{
  "version": "1.0",
  "serial": 3,
  "structs": {
    "StateGlobal": {
      "description": "状态机模块输出的全局状态",
      "owner": "state_module",
      "suffix": "OUT",
      "fields": [
        {"name": "head_power", "type": "uint8_t", "count": 4, "note": "4个炉头的功率档位 0-9"},
        {"name": "work_mode", "type": "uint8_t", "note": "系统工作模式"},
        {"name": "timer_remaining", "type": "uint16_t", "note": "定时剩余秒数"},
        {"name": "timer_active", "type": "uint8_t", "note": "定时是否激活 0/1"}
      ],
      "consumers": {
        "display_module": {
          "fields": ["head_power", "work_mode"]
        },
        "timer_module": {
          "fields": ["timer_remaining", "timer_active"]
        }
      }
    },

    "HeadState": {
      "description": "单个炉头状态快照",
      "owner": "state_module",
      "suffix": "OUT",
      "fields": [
        {"name": "zone", "type": "uint8_t", "note": "炉头编号 1-4"},
        {"name": "node", "type": "uint8_t", "note": "状态: 0=idle, 1=selecting, 2=cooking"},
        {"name": "power_level", "type": "uint8_t", "note": "当前档位 0-9"},
        {"name": "is_boost", "type": "uint8_t", "note": "强火标志"},
        {"name": "timer_set_ticks", "type": "uint16_t", "note": "定时设定值(ticks)"},
        {"name": "timer_value", "type": "uint16_t", "note": "定时剩余值(ticks)"},
        {"name": "select_ticks", "type": "uint16_t", "note": "选中倒计时(ticks)"}
      ],
      "consumers": {
        "display_module": {
          "fields": ["zone", "node", "power_level", "is_boost", "select_ticks"]
        },
        "timer_module": {
          "fields": ["zone", "node", "timer_set_ticks", "timer_value"]
        },
        "key_module": {
          "fields": ["zone", "node", "power_level"],
          "struct_name": "Key_HeadState"
        }
      },
      "deprecated": {
        "old_power": {
          "replaced_by": "power_level",
          "deadline": "2026-07-01",
          "note": "统一用 power_level 替代旧字段名"
        }
      }
    },

    "DisplayRefresh": {
      "description": "显示模块的输出，下发到硬件",
      "owner": "display_module",
      "suffix": "OUT",
      "fields": [
        {"name": "seg_chars", "type": "char", "count": 8},
        {"name": "seg_blink", "type": "uint8_t", "count": 4},
        {"name": "leds_power", "type": "uint8_t"},
        {"name": "leds_status", "type": "uint8_t"}
      ],
      "consumers": {}
    }
  }
}
```

---

## 三、generate_structs.py — 代码生成器

### 3.1 职责

读 `structs.json` → 生成每个模块的结构体 `.h` 文件。

### 3.2 生成规则

#### 规则 A：Owner 模块结构体

对于每个 struct，在其 owner 模块目录下生成/更新 `types.h`（如果模块已有 `types.h`，将生成内容合并进去）。

```c
/* AUTO-GENERATED from structs.json serial=3
   DO NOT EDIT. Run: python tools/generate_structs.py */

typedef struct {
    uint8_t  head_power[4];       /* 4个炉头的功率档位 0-9 */
    uint8_t  work_mode;           /* 系统工作模式 */
    uint16_t timer_remaining;     /* 定时剩余秒数 */
    uint8_t  timer_active;        /* 定时是否激活 0/1 */
} StateGlobal_OUT_t;
```

- 结构体名 = `{StructKey}_{suffix}_t`，如 suffix 为空则 `{StructKey}_t`
- 包含所有字段
- 每个字段带来源 JSON 的 `note` 注释

#### 规则 B：Consumer 模块结构体

对于 `consumers` 中的每个模块，在该模块目录下生成/更新 `types.h`。

```c
typedef struct {
    uint8_t head_power[4];        /* → state_module */
    uint8_t work_mode;            /* → state_module */
    /* PADDING: timer_remaining (2 bytes) */
    uint8_t __pad_0[2];
    /* PADDING: timer_active (1 byte) */
    uint8_t __pad_1[1];
} Display_StateInput_t;
```

- 结构体名 = `consumers.{module}.struct_name`（若未指定，则 `{Module}_{StructKey}_IN_t`）
- **只包含 `fields` 中声明的字段**
- **其余字段自动填 padding**：`uint8_t __pad_N[sizeof(跳过的字段)]`
- Padding 注释标明跳过了哪个源字段
- **字段顺序必须与 Owner struct 完全一致**，保证 `offsetof()` 相同

#### 规则 C：废弃字段处理

`deprecated` 中的字段：
- Owner struct: 仍包含，但注释中标注 `/* @deprecated: use {replaced_by} */`
- Consumer struct: 不生成（不会出现在任何 consumer 的 fields 中），如果字段本身被 consumer 引用则生成器**报错**

#### 规则 D：数组字段对齐

```json
{"name": "seg_chars", "type": "char", "count": 8}
```
生成：
```c
char seg_chars[8];
```

```json
{"name": "led_bitmap", "type": "uint32_t"}
```
生成：
```c
uint32_t led_bitmap;
```

#### 规则 E：类型白名单

生成器只接受以下 C 类型（嵌入式兼容）：

| 类别 | 允许的类型 |
|------|-----------|
| 整数 | `uint8_t`, `uint16_t`, `uint32_t`, `int8_t`, `int16_t`, `int32_t` |
| 字符 | `char` |
| 浮点 | `float` (需显式批准) |
| 枚举 | `uint8_t` 表示（枚举值用常量定义） |

类型的 `sizeof()` 由生成器内置表维护：`uint8_t`=1, `uint16_t`=2, `uint32_t`=4, `char`=1 等。

### 3.3 命令行接口

```bash
# 生成所有模块的结构体 .h 文件
python tools/generate_structs.py

# 检查模式: 生成到临时目录，与当前文件 diff
python tools/generate_structs.py --check

# 指定 structs.json 路径
python tools/generate_structs.py --source cfg/structs.json

# 只生成指定结构体
python tools/generate_structs.py --struct StateGlobal
```

### 3.4 --check 模式输出示例

```
$ python tools/generate_structs.py --check

  CHECK: state_module/types.h       — OK
  CHECK: display_module/types.h     — OK
  CHECK: timer_module/types.h       — MISMATCH

  --- timer_module/types.h (expected)
  +++ timer_module/types.h (actual)
  @@ -4,6 +4,6 @@
       uint16_t timer_remaining;
  -    uint8_t  timer_active;
  +    uint8_t  timer_active_flag;    ← 手动修改！与 structs.json 不一致

  RESULT: 1 mismatch found.
  请运行 python tools/generate_structs.py 重新生成。
```

### 3.5 与模块已有代码的合并

模块的 `types.h` 中可能既有 AUTO-GENERATED 区块，也有手写内容（如枚举常量、内部类型）：

```c
/* === 手写区域 === */
/* 本模块内部常量 */
#define IDX_DISPLAY_SEG_COUNT 8

/* === AUTO-GENERATED from structs.json serial=3 === */
/* DO NOT EDIT BELOW. Run: python tools/generate_structs.py */

typedef struct { ... } Display_StateInput_t;

/* === END AUTO-GENERATED === */
```

生成器只修改 `AUTO-GENERATED` / `END AUTO-GENERATED` 之间的内容。手写区域保持不动。

---

## 四、check_structs.py — 验证工具

### 4.1 职责

验证生成文件与 `structs.json` 的一致性。用于 pre-commit hook 和 CI。

### 4.2 实现

```python
# check_structs.py (≈100 行)
#
# 唯一逻辑:
#   1. 运行 generate_structs.py --check
#   2. 如果返回非零 → exit(1)
#   3. 如果返回零   → exit(0)
```

实际上 `check_structs.py` 可以就是一个包装器。但考虑到可能需要额外检查（如 structs.json 的 JSON schema 校验），独立文件有助于清晰划分职责。

### 4.3 检查项

| 检查项 | 说明 |
|--------|------|
| structs.json 格式 | JSON 语法有效，schema 字段齐全 |
| serial 单调性 | 当前 serial ≥ 所有生成文件内嵌的 serial |
| 生成一致性 | `generate_structs.py --check` 通过 |
| 废弃引用 | consumer 不引用 deprecated 字段 |
| 类型白名单 | 所有字段 type 在白名单内 |
| 模块存在性 | owner 和 consumers 中的模块名在项目目录中存在 |

---

## 五、AUTO-GENERATED 协约

### 5.1 生成文件的标识

每个生成的结构体 `.h` 文件头部包含：

```c
/* ================================================================
   AUTO-GENERATED from structs.json  serial=3  schema=1.0
   Generated: 2026-05-27 15:30:00
   DO NOT EDIT THIS FILE.
   
   修改跨模块结构体请编辑: cfg/structs.json
   然后运行:              python tools/generate_structs.py
   ================================================================ */
```

### 5.2 协约条款

1. **手工编辑生成文件即违规。** `check_structs.py` 在 pre-commit hook 中运行，检测到手动修改 → 提交被阻断。
2. **所有结构体变更从 JSON 开始。** JSON 是唯一被编辑的数据源。生成文件是只读产物。
3. **生成文件可被 `.gitignore` 忽略。** 因为可以从 JSON 确定性恢复。但建议提交以便于代码审查时查看结构体布局。
4. **新 AI 的上手路径：** 读到 AUTO-GENERATED 注释 → 知道不能改 → 找到 `structs.json` → 编辑 JSON → 重新生成。
5. **`serial` 防止回退。** JSON 的 serial 单调递增。生成文件的 serial 如果大于 JSON 的 serial，说明 JSON 被回退了但生成文件没重新生成 → `check_structs.py` 报错。

---

## 六、命名约定

### 6.1 后缀含义（数据流向）

| 后缀 | 所在模块角色 | 示例 | 谁读取 |
|------|------------|------|--------|
| `_OUT` | 本模块是**生产者** | `StateGlobal_OUT_t` | 通过 __weak 回调传给其他模块 |
| `_IN` | 本模块是**消费者** | `Display_StateInput_t` | 本模块通过 __weak 回调参数接收 |
| (无后缀) | 本模块**内部使用** | `DisplayBuffer_t` | 仅本模块，不跨模块 |

### 6.2 结构体命名

| 场景 | 命名格式 | 示例 |
|------|---------|------|
| Owner struct | `{StructKey}_{SUFFIX}_t` | `StateGlobal_OUT_t` |
| Consumer struct | `{Module}_{StructKey}_IN_t` | `Display_StateInput_t` |
| Consumer (自定义名) | JSON 中指定的 `struct_name` | `Key_HeadState_t` |

### 6.3 为什么后缀跟 Owner 不跟 Consumer

后缀描述的是**数据流向从谁的角度看**：

```c
// state_module/types.h  — state_module 是生产者
typedef struct { ... } StateGlobal_OUT_t;  // ← 这个模块对外输出

// display_module/types.h — display_module 是消费者  
typedef struct { ... } Display_StateInput_t; // ← 这个模块从外部接收
```

两个结构体是同一个数据的两种视图。Consumer 的 `_IN` 后缀表示"对我来说这是输入"。

---

## 七、与现有方法论的整合

### 7.1 三类文档的分工（都不参与编译）

项目维护**三类纯文档**，各司其职，没有一份被 C 代码 `#include`：

| 文档 | 内容 | 维护方式 | 验证工具 |
|------|------|---------|---------|
| `cfg/structs.json` | 结构体字段定义 + 消费关系 | 人/AI 手动编辑 | `generate_structs.py --check` |
| 生成 `types.h` | 每模块的结构体声明 | `generate_structs.py` 自动生成 | `check_structs.py` |
| `interface_map.h` | __weak 函数配对 + 签名 | AI 手动维护 | `check_weak_pairs.py` |

**关键**：前两者管"数据长什么样"，`interface_map.h` 管"谁把数据传给谁"。结构体变更只改 JSON，通道变更只改 `interface_map.h`。

### 7.2 文件布局

```
project/
├── cfg/
│   └── structs.json                ← 结构体定义 (唯一编辑源)
├── modules/
│   ├── state_module/
│   │   └── types.h                 ← AUTO-GENERATED
│   ├── display_module/
│   │   └── types.h                 ← AUTO-GENERATED
│   ├── timer_module/
│   │   └── types.h                 ← AUTO-GENERATED
│   └── key_module/
│       └── types.h                 ← AUTO-GENERATED
├── tools/
│   ├── generate_structs.py         ← JSON → types.h 生成器
│   ├── check_structs.py            ← 结构体一致性验证
│   ├── check_deps.py               ← 层依赖审计
│   └── check_weak_pairs.py         ← __weak 配对检查
├── docs/
│   └── interface_map.h             ← __weak 通道文档 (AI维护, 不参与编译)
└── .claude/
    └── specs/
        └── code-style.md           ← 含 _IN/_OUT 命名约定
```

### 7.3 修改场景：哪个改哪个

| 场景 | 改什么 | 跑什么工具 |
|------|-------|-----------|
| 新增/删除结构体字段 | `structs.json` | `generate_structs.py` |
| 新增 consumer 模块 | `structs.json` | `generate_structs.py` |
| 废弃字段 | `structs.json` `deprecated` | `generate_structs.py` |
| 新增 __weak 通道 | `interface_map.h` + 代码 | `check_weak_pairs.py` |
| 修改 __weak 函数签名 | `interface_map.h` + 代码 | `check_weak_pairs.py` |
| 同时改结构体+通道 | 两个都改 | 全部跑 |

### 7.4 修改方法论文档

#### 05-interface-management.md 需要新增章节：

```
## 六、跨模块结构体生成规范

跨模块结构体不使用注释标签管理，而是通过生成式工具保证一致性:

1. **单一数据源**: cfg/structs.json
2. **代码生成**: python tools/generate_structs.py
3. **验证**: python tools/check_structs.py (pre-commit hook)
4. **修改流程**:
   - 编辑 structs.json (添加/删除/修改字段, 添加consumer)
   - 运行 generate_structs.py
   - 如涉及 __weak 回调签名变更: 更新 interface_map.h
   - 运行 check_weak_pairs.py + check_deps.py
   - armcc 编译

独立类型声明铁律仍然适用，但现在一致性由生成器保证，
不需要 AI 手动维护 sizeof/offsetof 相等。
```

#### 01-architecture.md 铁律三修正：

```
### 铁律三: 独立声明，生成器管一致性  (修正)

跨模块传递的数据结构，由 generate_structs.py 从 cfg/structs.json 
自动生成每个模块的独立声明。相同内存布局，不同命名。
一致性由生成器保证，验证由 check_structs.py 兜底。

⚠️ 禁止手动编辑 AUTO-GENERATED 区块。
```

### 7.3 Pre-commit Hook

```bash
#!/bin/bash
# .git/hooks/pre-commit

set -e

echo "=== 结构体一致性检查 ==="
python tools/check_structs.py || exit 1

echo "=== __weak 配对检查 ==="
python tools/check_weak_pairs.py || exit 1

echo "=== 层依赖审计 ==="
python tools/check_deps.py ../Claude || exit 1

echo "=== 全部通过 ==="
```

---

## 八、与原方案的对比

| 维度 | 原方案（注释驱动 V2.0） | 本方案（生成式 V1.0） |
|------|------------------------|---------------------|
| **数据源** | 散落在各 .h 的 14 种注释标签 | 单一 `structs.json` |
| **操作者负担** | 需理解 14 种标签语法和生命周期 | 只需理解 JSON schema (~5 个字段) |
| **一致性保证** | 依赖标签被正确写入和解析 | 从单一数据源确定性生成 |
| **验证方式** | 解析 C 注释 + typedef + 字段级 diff | `generate_structs.py --check` (重新生成并 diff) |
| **跨 AI 可复制性** | 低: 新 AI 必须学会标签语法 | 高: 新 AI 读到 AUTO-GENERATED 就知道怎么走 |
| **误操作后果** | 标签写错 → 工具静默生成错误结构体 | JSON 编辑错误 → 生成失败/编译失败（立即暴露） |
| **工具复杂度** | ~1000 行 (扫描+解析+执行+清空+归档) | ~500 行 (JSON schema 校验 + 代码生成 + diff) |
| **归档/回滚** | 自建管理链表 + @rollback | `git log structs.json` + `git revert` |
| **废弃字段** | `@deprecate` 标签 | JSON `deprecated` 字段，生成期阻断引用 |
| **字段级裁剪** | `@import` 标签 | JSON `consumers.fields` |
| **padding 管理** | 手动 `@reserve` | 工具自动生成 `__pad_N` |
| **编译期隔离** | 注释掉 `#ifndef` (对 typedef 无效) | `check_deps.py` 增强: 检测私有 .h 多头引用 |

---

## 九、实现计划

### Phase 1: Schema 定型 + 文档 (0.5h)
- [ ] 确定 structs.json 最终 schema
- [ ] 写 `docs/architecture/struct-generation-spec.md`
- [ ] 更新 `methodology-seed/05-interface-management.md`
- [ ] 更新 `methodology-seed/01-architecture.md` 铁律三
- [ ] 更新 `methodology-seed/templates/code-style.md` (加 `_IN`/`_OUT` 约定)

### Phase 2: 生成器 (2h)
- [ ] `tools/generate_structs.py`
  - JSON schema 校验
  - Owner struct 生成
  - Consumer struct 生成（自动 padding）
  - AUTO-GENERATED 标记生成
  - --check 模式（临时目录生成 + diff）
  - 手写区域保护（AUTO-GENERATED 区块替换）

### Phase 3: 验证工具 (0.5h)
- [ ] `tools/check_structs.py`
  - 调用 generate_structs.py --check
  - serial 单调性检查
  - deprecated 引用检查
  - 非零退出码 → pre-commit 阻断

### Phase 4: 移植现有项目 (1h)
- [ ] 将当前 4 模块的跨模块结构体录入 `structs.json`
- [ ] 运行 generate_structs.py 生成 types.h
- [ ] 修改各模块 `#include` 指向新的 types.h
- [ ] 运行完整测试套件 (35/35 WASM + armcc 0e0w)

### Phase 5: 方法论种子更新 (0.5h)
- [ ] 复制 `generate_structs.py` + `check_structs.py` 到 `methodology-seed/tools/`
- [ ] 复制 `structs.json` 模板到 `methodology-seed/templates/`
- [ ] 更新 `methodology-seed/ONBOARDING.md` 增加生成步骤
- [ ] 更新 `methodology-seed/MEMORY.md`

---

## 十、不覆盖的场景

以下场景明确排除在 V1.0 范围外：

- **嵌套结构体**: struct 内字段类型是另一个 struct。V1.0 只支持基本 C 类型。
- **联合体 (union)**: 不使用。
- **位域 (bit-field)**: 嵌入式不建议，用 `uint8_t` + 掩码代替。
- **函数指针字段**: 跨模块传递函数指针违反解耦原则，不允许。
- **多级数组**: 只支持一维数组。
- **条件字段**: 根据宏定义切换字段布局。过于复杂，保持简单。

---

*V1.0, 2026-05-27*
